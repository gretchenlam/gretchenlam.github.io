Drop personal photos into these folders:

- `favorites`
- `travel`
- `hobbies`

Only images inside those three album folders are used by the Photos app. Files placed directly in `public/img/photos` are ignored.

Then run:

```sh
npm run photos
```

Supported file types: `.jpg`, `.jpeg`, `.png`, `.webp`, `.gif`, `.avif`.
